version: "3.8"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: chat
      POSTGRES_USER: chat
      POSTGRES_PASSWORD: chat
  ports:
   -"5432:5432"

redis:
  image: redis:7
  ports:
   -"6379:6379"